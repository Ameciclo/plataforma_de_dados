{
  "compilerOptions": {
    "target": "es6",
    "module": "commonjs",
    "rootDir": "./src",
    "outDir": "./dist",
    "esModuleInterop": true,
    "strict": true,
    // The folder or files you want to exclude
    // You can also use regex `**/*.spec.ts` for ex
    "exclude":[
      "node_modules",
      "build"
    ]
  }
}
